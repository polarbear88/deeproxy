## Handoff: team-plan → team-exec

- **Decided**: 3-worker team, partitioned by DISJOINT file ownership (no worktrees needed). worker-backend owns all *.go + CLAUDE.md/docs (coupled core, single owner per architect's hot-file rule); worker-fe-views owns non-dashboard .vue (user-mgmt/ProxyGroups/Settings) + their api clients; worker-fe-dash owns Dashboard.vue/EChart.js/theme.js/router/index.js. Frontend builds against the plan's documented API contracts in parallel with backend.
- **Decided (consensus, from ralplan v5)**: config=A1 write-before-commit via rule.BuildGroupEngine (NOT Rebuild, no Tx); all_groups=coexist boolean (never clear group_user); relay=wait-both + close-both-on-error-only inside relayCounted; health=global semaphore pool default 150 read per-scan; bulk upstream=single SQL UPDATE WHERE/IN-chunked; authz log at Credential.Valid only.
- **Rejected**: A2 Tx-transactional config (store is single-writer SetMaxOpenConns(1), no Tx read path); worktree isolation (file sets disjoint, unnecessary merge overhead); select-first-complete relay (reintroduces truncation bug).
- **Risks**: (1) download truncation if relay error-path misimplemented — hard regression test gates it. (2) api/server.go routes + snapbuild/rebuild.go touched by multiple backend tasks — mitigated by single backend worker serial. (3) frontend/backend API contract drift — integration verified at team-verify. (4) migration must be idempotent (T0.0) — no ALTER primitive existed.
- **Files**: plan=.omc/plans/ralplan-v2-batch-fixes.md, spec=.omc/specs/deep-interview-v2-batch-fixes.md. Tasks #1-6 backend (2-6 blockedBy #1=WP-0), #7-9 fe-views (#9 blockedBy #7), #10-11 fe-dash.
- **Remaining**: team-verify must include security-reviewer (auth/authz changes) + code-reviewer (>20 files); run go build/vet/test + npm run build; verify all 29 ACs; end-to-end authz放行/拒绝 + 日志, relay 不断下载.
